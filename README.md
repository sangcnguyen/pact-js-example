# PactJS Contract Testing Example

An example test using PactJs to validate contract testing between consumer and provider. All data generated by provider is dummy.

## How to run a contract

Install dependencies
`npm i`

Run the consumer tests
`npm run test:consumer`

Publish the contract to your pact broker
`npm run publish:contract`

Start & verify contract from provider side
`npm run test:provider`
